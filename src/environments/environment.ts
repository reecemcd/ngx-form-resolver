// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  urls: {
    ex1: 'https://raw.githubusercontent.com/reecemcd/ngx-form-resolver/master/src/app/examples/1-basic-form-resolver/example1',
    ex2: 'https://raw.githubusercontent.com/reecemcd/ngx-form-resolver/master/src/app/examples/2-dynamic-form-resolver/example2',
    ex3: 'https://raw.githubusercontent.com/reecemcd/ngx-form-resolver/master/src/app/examples/3-custom-form-control-resolver/example3',
    ex4: 'https://raw.githubusercontent.com/reecemcd/ngx-form-resolver/master/src/app/examples/4-form-control-resolver-parameters/example4',
  }
};


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
